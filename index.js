const {
    clear,
    getContentType,
    readText,
    readHTML,
    writeText,
    readImage,
    writeImage,
    readFiles,
    writeFiles,
    saveImageToFile,
    getFileIcon
} = require('node-gyp-build')(__dirname)

module.exports = {
    clear,
    getContentType,
    readText,
    readHTML,
    writeText,
    readImage,
    writeImage,
    readFiles,
    writeFiles,
    saveImageToFile,
    getFileIcon
}
